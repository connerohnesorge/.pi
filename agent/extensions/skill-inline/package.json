{
	"name": "pi-inline-skill-autocomplete",
	"version": "0.1.0",
	"description": "Inline / skill autocomplete for pi, so skills complete anywhere in the editor instead of only at command start.",
	"type": "module",
	"keywords": [
		"pi-package",
		"pi",
		"autocomplete",
		"skills",
		"slash-commands"
	],
	"license": "ISC",
	"files": [
		"main.js",
		"README.md"
	],
	"engines": {
		"node": ">=18"
	},
	"scripts": {
		"check": "npm run build && npm run test && npm run pack:dry",
		"test": "node --test",
		"verify": "npm run check",
		"build": "node --check main.js",
		"pack:dry": "npm pack --dry-run"
	},
	"pi": {
		"extensions": [
			"./main.js"
		]
	},
	"publishConfig": {
		"access": "public"
	}
}
